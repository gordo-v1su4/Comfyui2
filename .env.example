# ComfyUI + MinIO configuration (example)
# Copy to .env and set real values. Do NOT commit .env.

# Enable S3 integration in the container
COMFYUI_S3_ENABLED=true

# MinIO endpoint (API URL)
# Example for your external MinIO in production:
# MINIO_ENDPOINT=https://minio-api.v1su4.com
# Example for local dev (if you run MinIO locally):
# MINIO_ENDPOINT=http://minio:9000
MINIO_ENDPOINT=

# MinIO credentials
SERVICE_USER_MINIO=
SERVICE_PASSWORD_MINIO=

# Bucket where models are stored
MINIO_BUCKET_MODELS=ai-models


CLI_ARGS=--listen 0.0.0.0 --port 8188 --enable-cors-header "*"
COMFYUI_S3_ENABLED=true
HF_TOKEN=hf_
MINIO_ACCESS_KEY=
MINIO_BUCKET_MODELS=ai-models
MINIO_ENDPOINT=
MINIO_SECRET_KEY=
NVIDIA_DRIVER_CAPABILITIES=compute,utility
NVIDIA_VISIBLE_DEVICES=all
PYTHONUNBUFFERED=1
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512
SERVICE_PASSWORD_MINIO=
SERVICE_USER_MINIO=